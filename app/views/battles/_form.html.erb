<%= form_for @battle do |f| %>

    <%= f.hidden_field :user_id%>
    <%= f.label :opponent%>
    <%= f.collection_select :opponent_id, Trainer.all, :id, :name%>
    <%= f.submit "Start Battle" %>
    <% end %>